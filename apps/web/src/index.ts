/**
 * @threadmark/web — Next.js UI (thin: no business logic). Uploads, the PRD
 * editor (Tiptap + Yjs), comments, branch comparison, and agent-run
 * observability panels.
 *
 * Placeholder for the scaffold. Real Next.js app configuration and pages land
 * when the web UI is built (see plan; observability UI is PR10).
 */
export const APP_NAME = '@threadmark/web';
