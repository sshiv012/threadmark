/**
 * @threadmark/worker — Temporal worker hosting deterministic workflows and
 * side-effecting activities (ingestion, retrieval, rerank, generation,
 * indexing). Workflows stay deterministic; all network/DB/model/filesystem
 * side effects live in activities. Retryable operations are idempotent.
 *
 * Placeholder for the scaffold. The ingestion workflow lands in PR5.
 */
export const APP_NAME = '@threadmark/worker';
