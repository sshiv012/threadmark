{
  "name": "@threadmark/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "JWT_SECRET=test-secret-do-not-use-in-prod vitest run --passWithNoTests",
    "api": "node --env-file=../../.env dist/index.js"
  },
  "dependencies": {
    "@threadmark/db": "workspace:^",
    "@threadmark/telemetry": "workspace:^",
    "fastify": "^5.10.0",
    "jose": "^6.2.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "@electric-sql/pglite-pgvector": "^0.0.5",
    "@opentelemetry/api": "^1.9.1",
    "@opentelemetry/sdk-trace-base": "^2.10.0",
    "@opentelemetry/sdk-trace-node": "^2.10.0",
    "drizzle-orm": "^0.45.2"
  }
}
