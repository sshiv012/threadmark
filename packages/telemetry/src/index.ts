/**
 * @threadmark/telemetry — OpenTelemetry setup and helpers for projecting
 * workflow-run state (agent_run / agent_step) so every important state
 * transition is observable in the UI. Retries, errors, tool calls, and
 * citations are never hidden.
 *
 * Placeholder export for the scaffold.
 */
export const PACKAGE_NAME = '@threadmark/telemetry';
