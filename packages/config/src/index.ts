/**
 * @threadmark/config — shared, validated configuration loading (provider
 * selection, connection strings, feature flags). Config is parsed once with
 * Zod so misconfiguration fails fast at startup.
 *
 * Placeholder export for the scaffold.
 */
export const PACKAGE_NAME = '@threadmark/config';
